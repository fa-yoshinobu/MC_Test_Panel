namespace McTestPanel;

public enum DeviceType
{
    Bit,
    Word,
}

public enum DeviceMode
{
    Momentary,
    Alternate,
    Lamp,
    Value,
    SetConst,
    Inc,
    Dec,
    Display,
}

public enum RadixMode
{
    Dec,
    Hex,
}
